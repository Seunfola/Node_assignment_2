//import the sum function from the math.js module using require
 const {sum,calculator,subtract} = require('./math.js');

//  const calculator = require("./math");

 //Call the sum function with two numbers and log the result to the console
  console.log(sum(5,7));
  //Call the multiply method on the calculator object with two numbers and log the result to the console

  console.log(calculator(2, 3));

console.log(subtract(14,5))