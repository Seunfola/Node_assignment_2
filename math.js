//define a function called sum that takes two arguments and returns their sum.

const sum =(a,b) => a+b;

const calculator =(a, b) => {
  //Add a method to the calculator object called multiply
  multiply: a * b
};

const subtract =(a,b)=>a-b
//Export the sum function so that it can be used in another module
module.exports = { sum, subtract, calculator };
